# Pinned upstream versions for third_party/ dependencies. Sourced by
# both scripts/build-manifold.sh (native + cross) and scripts/build-wasm.sh
# (browser) so the desktop and web builds always pull the same manifold/
# assimp/freetype tags. Do not duplicate these constants.
MANIFOLD_VERSION="v3.3.2"
ASSIMP_VERSION="v5.4.3"
FREETYPE_VERSION="VER-2-13-3"
